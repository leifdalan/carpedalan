version: '2.4'


services:
 
  cypress:
    tty: true
    links:
      - "alb:local.carpedalan.com"
      - "alb:photos.local.carpedalan.com"
      - "alb:cdn.local.carpedalan.com"
    environment:
      CYPRESS_BASE_URL: http://local.carpedalan.com
    env_file: 
      - .env.ci
    image: registry.gitlab.com/carpedalan/carpedalan-web/cypress:latest
    depends_on:
      api: 
        condition: service_healthy
      alb: 
        condition: service_started
    command: yarn test
    volumes: 
      - ./cypress:/cy/cypress
      - ./.env.example:/cy/.env.example
      - ./.env.ci:/cy/.env
      - ./db/:/cy/db/
      - ./shared/:/cy/shared/

  alb:
    extends:
      file: docker-compose.base.yml
      service: alb
    volumes: 
      - ./nginx/nginx-e2e.conf:/etc/nginx/nginx.conf
      - ./client/dist:/assets/

    depends_on:
      api: 
        condition: service_healthy
      client: 
        condition: service_healthy
      aws: 
        condition: service_started


  client:
    stdin_open: true
    tty: true
    image: client:latest
    extends:
      file: docker-compose.base.yml
      service: client
    volumes:
      - ./client/jest.config.js:/app/client/jest.config.js
      - ./client/setupTests.js:/app/client/setupTests.js
      - ./client/dist:/app/client/dist
      - ./client/tsconfig.test.json:/app/client/tsconfig.test.json
    env_file: 
      - .env.ci
    environment: 
      PROD_BUILD: 'true'
      PORT: "${CLIENT_PORT-4000}"

  api:
    stdin_open: true
    tty: true
    image: api:latest
    extends:
      file: docker-compose.base.yml
      service: api
    volumes:
      - './server:/app/server:delegated'
      - './scripts:/app/scripts:delegated'
      - './client/dist:/app/server/dist'
      - './index.js:/app/index.js:delegated'
      - './db:/app/db:delegated'
      - './shared:/app/shared:delegated'
      - './.env.ci:/app/.env'
    depends_on:
      pg:
        condition: service_healthy
      client:
        condition: service_healthy

    environment:
      PORT: 8001
      PROD_BUILD: 'true'
      ASSET_CDN_DOMAIN: cdn.local.carpedalan.com
    ports: 
      - '8001:8001'
  pg:
    extends:
      file: docker-compose.base.yml
      service: pg
  aws:
    extends:
      file: docker-compose.base.yml
      service: aws


